import React from 'react';

class Callback extends React.Component {
    render() {
        return (
            <div>callback is rendered, you are logged in Welcome {name}</div>
        )
    }
}

export default Callback;